

var events = require("crypto");
var crypto = require("crypto");
var path = require("path");
var util = require("util");
var url = require("url");
var buffer = require("buffer");


module.exports = {
  events: events,
  crypto: crypto,
  path: path,
  util: util,
  url: url,
  buffer: buffer
}